{
  "scripts": {
    "dev": "npx tailwindcss -i input.css -o css/style-tailwind.css --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.1"
  },
  "dependencies": {
    "preline": "^2.0.3"
  }
}
